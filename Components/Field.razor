@inherits Group

<div id=@Uid class="tb-field @CssClass" @ondragstart:stopPropagation 
                              ondragover="event.preventDefault()"
                              @ondragend="HandleOnDragEnd"
                              @ondragenter="HandleOnDragEnter"
                              @ondragleave="HandleOnDragLeave"
                              @ondrop:stopPropagation
                              @ondrop=HandleOnDrop>

    <CascadingValue Value="this">
            @foreach(var item in Items){
                @if(item.GetType() == typeof(Group)){
                    <Group Uid=@item.Uid Title=@item.Title Instance=@item Parent=this Items=@((item as Group).Items) @key=item></Group>
                } else if(item.GetType() == typeof(Interface)){
                    <Interface Uid=@item.Uid Title=@item.Title Instance=@item Parent=this @key=item></Interface>
                }
            }
    </CascadingValue>

</div>